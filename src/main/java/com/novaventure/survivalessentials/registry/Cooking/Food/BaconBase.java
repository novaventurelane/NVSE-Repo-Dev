package com.novaventure.survivalessentials.registry.Cooking.Food;

import net.minecraft.item.Item;

public class BaconBase extends Item {
    public BaconBase(Settings settings) {
        super(settings);
    }
}
